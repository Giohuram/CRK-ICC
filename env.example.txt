# Renommer ce fichier en .env.local pour le développement local
# ou configurer ces variables dans Vercel > Settings > Environment Variables

# Connection string Neon PostgreSQL
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

# Mot de passe de l'espace admin (défaut : crk-icc-demo)
ADMIN_PASSWORD="crk-icc-demo"
